;; Simple program to store input. Start it and then
;; put your input into r1. Set the r0 to store
;; your input in r2

; Continuously check r0 until it is set then jump to 11
tst 0
--jmp 10
jmp 1

; Move the value stored in r1 to r2, then jump back to the start (1)
tst 1
--jmp 7
jmp 1
dec 1
inc 2
jmp 4

; Set r0 to 0 again
; Make sure r2, the register to store it in, is empty. 
; Decrement until it's empty, then jump to 4
dec 0
tst 2
--jmp 14
jmp 4
dec 2
jmp 11