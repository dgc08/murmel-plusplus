;!MURBIN

;include std.main
include std.io

$msg1:
    #"Hello, world!"
$msg2:
    #"Good day, computer!"

_start:
    ; mov r0 $msg
    ; putmacro putln
    call world
    hlt 42

world:
    mov r0 $msg1
    putmacro putln ; just puts + print newline
    call computer
    ret

computer:
    mov r0 $msg2
    putmacro putln
    ret
